# Minimal topology demo

Apply it from the tools container:

```sh
/workspace/workloads/topology-demo/apply.sh
```

The frontend calls the single API route every five seconds. The API proxies that route to the backend's single response endpoint.
